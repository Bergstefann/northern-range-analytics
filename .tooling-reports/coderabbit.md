# CodeRabbit — NOT INSTALLED — pass skipped

`coderabbit@claude-plugins-official` plugin is installed, but the standalone `coderabbit` CLI binary that the plugin's `code-review` skill depends on is not present on this machine (`which coderabbit` → not found), and it isn't authenticated. Per the skill's own prerequisites check, this pass cannot run.

No output was generated or approximated for this pass.
